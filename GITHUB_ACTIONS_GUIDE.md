# 🚀 คู่มือการใช้ GitHub Actions สำหรับ Build APK

## 📋 ภาพรวม

คู่มือนี้จะแนะนำวิธีการใช้ **GitHub Actions** เพื่อ build APK อัตโนมัติสำหรับเกม Ghost Catching Game **โดยไม่ต้องติดตั้งอะไรบนเครื่องเลย!**

---

## ✅ ข้อดีของ GitHub Actions

- ✅ **ฟรี 100%** (2,000 นาทีต่อเดือน)
- ✅ **Build บน Cloud** - ไม่ต้องใช้เครื่องตัวเอง
- ✅ **อัตโนมัติ** - Push โค้ด → Build APK
- ✅ **ไม่ต้องติดตั้ง** - ไม่ต้องมี Android SDK/NDK
- ✅ **ใช้ได้ตลอด** - Build ได้ไม่จำกัดครั้ง

---

## 🎯 ขั้นตอนที่ 1: สร้าง GitHub Account

### ถ้ามี GitHub Account แล้ว → ข้ามไปขั้นตอนที่ 2

### ถ้ายังไม่มี:

1. ไปที่ https://github.com/signup
2. กรอกข้อมูล:
   - **Email**: อีเมลของคุณ
   - **Password**: รหัสผ่าน (อย่างน้อย 8 ตัวอักษร)
   - **Username**: ชื่อผู้ใช้ (ภาษาอังกฤษ)
3. คลิก **"Create account"**
4. ยืนยันอีเมล (เช็คกล่องจดหมาย)
5. เสร็จ! ✅

---

## 🎯 ขั้นตอนที่ 2: สร้าง Repository

### 2.1 เข้าสู่ระบบ GitHub
- ไปที่ https://github.com
- Login ด้วย Username และ Password

### 2.2 สร้าง Repository ใหม่
1. คลิกปุ่ม **"+"** มุมขวาบน
2. เลือก **"New repository"**
3. กรอกข้อมูล:
   - **Repository name**: `ghost-catching-game`
   - **Description**: `A puzzle game where you trap ghosts!`
   - **Public** หรือ **Private**: เลือกตามใจชอบ
   - ✅ เลือก **"Add a README file"**
4. คลิก **"Create repository"**

---

## 🎯 ขั้นตอนที่ 3: อัปโหลดโค้ดไป GitHub

### วิธีที่ 1: ใช้ GitHub Web Interface (ง่ายที่สุด!)

#### 3.1 ดาวน์โหลดไฟล์โปรเจกต์
- ดาวน์โหลด `ghost_catching_game_with_admob.tar.gz` ที่ผมส่งให้
- แตกไฟล์ออกมา

#### 3.2 อัปโหลดไฟล์ทีละโฟลเดอร์

**อัปโหลดโฟลเดอร์ `.github`**:
1. เข้า Repository ที่สร้างไว้
2. คลิก **"Add file"** → **"Upload files"**
3. ลากโฟลเดอร์ `.github` ทั้งโฟลเดอร์มาวาง
4. กรอก Commit message: `Add GitHub Actions workflow`
5. คลิก **"Commit changes"**

**อัปโหลดโฟลเดอร์ `assets`**:
1. คลิก **"Add file"** → **"Upload files"**
2. ลากโฟลเดอร์ `assets` มาวาง
3. กรอก Commit message: `Add game assets`
4. คลิก **"Commit changes"**

**อัปโหลดโฟลเดอร์ `src`**:
1. คลิก **"Add file"** → **"Upload files"**
2. ลากโฟลเดอร์ `src` มาวาง
3. กรอก Commit message: `Add source code`
4. คลิก **"Commit changes"**

**อัปโหลดไฟล์หลัก**:
1. คลิก **"Add file"** → **"Upload files"**
2. ลากไฟล์เหล่านี้มาวาง:
   - `main.py`
   - `main_v2.py`
   - `buildozer.spec`
   - `requirements.txt`
   - `.gitignore`
   - `README_GITHUB.md` (เปลี่ยนชื่อเป็น `README.md`)
3. กรอก Commit message: `Add main files`
4. คลิก **"Commit changes"**

---

### วิธีที่ 2: ใช้ Git Command Line (สำหรับผู้ชำนาญ)

```bash
# 1. ติดตั้ง Git (ถ้ายังไม่มี)
sudo apt install git

# 2. ตั้งค่า Git
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 3. เข้าไปยังโฟลเดอร์เกม
cd /path/to/ghost_catching_game

# 4. Initialize Git
git init

# 5. เพิ่มไฟล์ทั้งหมด
git add .

# 6. Commit
git commit -m "Initial commit"

# 7. เชื่อมต่อกับ GitHub
git remote add origin https://github.com/YOUR_USERNAME/ghost-catching-game.git

# 8. Push ขึ้น GitHub
git branch -M main
git push -u origin main
```

---

## 🎯 ขั้นตอนที่ 4: รัน GitHub Actions

### 4.1 ตรวจสอบว่า Workflow ทำงาน

1. เข้า Repository บน GitHub
2. คลิกแท็บ **"Actions"** (ด้านบน)
3. คุณจะเห็น Workflow ชื่อ **"Build Android APK"**
4. ถ้ามีสีเหลือง 🟡 = กำลัง build อยู่
5. ถ้ามีสีเขียว ✅ = build สำเร็จ!
6. ถ้ามีสีแดง ❌ = build ล้มเหลว (ดู logs)

### 4.2 รัน Workflow ด้วยตนเอง (ถ้ายังไม่ทำงาน)

1. ไปที่แท็บ **"Actions"**
2. คลิก **"Build Android APK"** (ด้านซ้าย)
3. คลิกปุ่ม **"Run workflow"** (ด้านขวา)
4. เลือก branch: **main**
5. คลิก **"Run workflow"** สีเขียว
6. รอ 30-60 นาที

---

## 🎯 ขั้นตอนที่ 5: ดาวน์โหลด APK

### 5.1 รอ Build เสร็จ

- ดูสถานะที่แท็บ **"Actions"**
- เมื่อเห็นเครื่องหมาย ✅ สีเขียว = เสร็จแล้ว!

### 5.2 ดาวน์โหลด APK

1. คลิกเข้าไปที่ Workflow run ที่สำเร็จ
2. เลื่อนลงมาด้านล่างสุด
3. ในส่วน **"Artifacts"** คุณจะเห็น:
   - 📦 **ghost-catching-game-debug**
4. คลิกดาวน์โหลด
5. แตกไฟล์ ZIP
6. คุณจะได้ไฟล์ `ghostcatchinggame-1.0.0-debug.apk`

---

## 🎯 ขั้นตอนที่ 6: ติดตั้ง APK บนมือถือ

### 6.1 ส่งไฟล์ APK ไปยังมือถือ

**วิธีที่ 1**: ใช้ Google Drive
1. อัปโหลด APK ไป Google Drive
2. เปิด Google Drive บนมือถือ
3. ดาวน์โหลด APK

**วิธีที่ 2**: ใช้ Email
1. ส่ง APK ไปที่อีเมลตัวเอง
2. เปิดอีเมลบนมือถือ
3. ดาวน์โหลด APK

**วิธีที่ 3**: ใช้ USB Cable
1. เชื่อมต่อมือถือกับคอมพิวเตอร์
2. คัดลอก APK ไปยังมือถือ
3. เปิด File Manager บนมือถือ
4. หาไฟล์ APK และติดตั้ง

### 6.2 เปิดการติดตั้งจากแหล่งที่ไม่รู้จัก

1. ไปที่ **Settings** → **Security**
2. เปิด **"Install from unknown sources"** หรือ **"Allow from this source"**

### 6.3 ติดตั้ง APK

1. เปิดไฟล์ APK บนมือถือ
2. คลิก **"Install"**
3. รอการติดตั้งเสร็จ
4. คลิก **"Open"**
5. เล่นเกม! 🎮

---

## 🔄 การอัปเดตเกม (ในอนาคต)

### เมื่อต้องการแก้ไขเกมหรือเพิ่มฟีเจอร์:

1. แก้ไขโค้ดในเครื่อง
2. อัปโหลดไฟล์ที่แก้ไขไป GitHub:
   - คลิก **"Add file"** → **"Upload files"**
   - เลือกไฟล์ที่แก้ไข
   - กรอก Commit message: `Fix bug` หรือ `Add new feature`
   - คลิก **"Commit changes"**
3. GitHub Actions จะ build APK ใหม่อัตโนมัติ!
4. ดาวน์โหลด APK เวอร์ชันใหม่จาก Artifacts

---

## 🐛 แก้ปัญหาที่พบบ่อย

### ปัญหา: Build ล้มเหลว (❌ สีแดง)

**วิธีแก้**:
1. คลิกเข้าไปที่ Workflow run ที่ล้มเหลว
2. คลิกที่ **"build"** job
3. อ่าน error message
4. แก้ไขตาม error
5. Push โค้ดใหม่

### ปัญหา: Build ช้ามาก (>60 นาที)

**สาเหตุ**: การ build ครั้งแรกจะช้าเพราะต้องดาวน์โหลด Android SDK/NDK

**วิธีแก้**: รอให้เสร็จ ครั้งต่อไปจะเร็วขึ้น (ใช้ cache)

### ปัญหา: ไม่เห็น Artifacts

**วิธีแก้**:
- ตรวจสอบว่า build สำเร็จ (✅ สีเขียว)
- Artifacts จะอยู่ด้านล่างสุดของหน้า Workflow run
- ถ้ายังไม่เห็น ลองรีเฟรชหน้า

### ปัญหา: APK ติดตั้งไม่ได้บนมือถือ

**วิธีแก้**:
- ตรวจสอบว่าเปิด "Install from unknown sources" แล้ว
- ลบ APK เวอร์ชันเก่าออกก่อน (ถ้ามี)
- ตรวจสอบว่ามือถือรองรับ Android 5.0+ (API 21+)

---

## 📊 ข้อมูล GitHub Actions

### Free Tier Limits:
- **2,000 นาที/เดือน** สำหรับ Public repositories
- **500 MB** Artifacts storage
- **Unlimited** สำหรับ Public repositories

### เวลาที่ใช้:
- **Build ครั้งแรก**: 40-60 นาที
- **Build ครั้งต่อไป**: 20-30 นาที (ใช้ cache)

### ขนาด APK:
- **Debug APK**: ~20-30 MB
- **Release APK**: ~15-20 MB (หลัง optimize)

---

## 💡 Tips & Tricks

### 1. ใช้ Cache เพื่อเร่งความเร็ว
Workflow ที่ผมสร้างให้มี cache อยู่แล้ว ทำให้ build ครั้งต่อไปเร็วขึ้น

### 2. Build เฉพาะเมื่อต้องการ
แก้ไข workflow file:
```yaml
on:
  workflow_dispatch:  # เฉพาะเมื่อกดปุ่ม Run workflow
```

### 3. ดู Logs เพื่อ Debug
- คลิกเข้าไปที่ Workflow run
- คลิกที่ Step ที่ต้องการดู
- อ่าน logs ละเอียด

### 4. ใช้ Secrets สำหรับ Keystore
เมื่อต้องการ build Release APK:
1. ไปที่ **Settings** → **Secrets and variables** → **Actions**
2. คลิก **"New repository secret"**
3. เพิ่ม Keystore เป็น Base64 encoded string

---

## 🎓 เรียนรู้เพิ่มเติม

### GitHub Actions Documentation:
- https://docs.github.com/en/actions

### Buildozer Documentation:
- https://buildozer.readthedocs.io/

### GitHub Learning Lab:
- https://lab.github.com/

---

## ✅ Checklist

- [ ] สร้าง GitHub Account
- [ ] สร้าง Repository
- [ ] อัปโหลดโค้ดไป GitHub
- [ ] ตรวจสอบ Workflow ทำงาน
- [ ] รอ Build เสร็จ (30-60 นาที)
- [ ] ดาวน์โหลด APK จาก Artifacts
- [ ] ติดตั้ง APK บนมือถือ
- [ ] ทดสอบเกม
- [ ] เผยแพร่ไป Google Play Store!

---

## 🎉 สำเร็จ!

ตอนนี้คุณสามารถ build APK ได้โดยไม่ต้องติดตั้งอะไรเลย!

**แค่ push โค้ด → รอ 30 นาที → ได้ APK!** 🚀

---

**ขอให้โชคดีกับการพัฒนาเกมครับ!** 🎮✨
